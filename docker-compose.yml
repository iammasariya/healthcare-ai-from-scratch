version: '3.8'

services:
  api:
    build: .
    container_name: healthcare-ai-service
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
      - DEBUG=false
    volumes:
      # Mount app code for development (remove in production)
      - ./app:/app/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  ui:
    build:
      context: ./ui
    container_name: healthcare-ai-ui
    ports:
      - "3000:3000"
    environment:
      API_UPSTREAM: http://api:8000
      FRAME_ANCESTORS: "'self' https://*.epic.com"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

  # Optional: Add nginx reverse proxy for production
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   depends_on:
  #     - api
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   restart: unless-stopped
